<snippet>
  <content><![CDATA[
protected function replace${1:Foo}(\$message, \$attribute, \$rule, \$parameters)
{
  return str_replace(':${2:foo}', \$parameters[0], \$message);
}
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>val--replaceMsg</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.php</scope>
</snippet>